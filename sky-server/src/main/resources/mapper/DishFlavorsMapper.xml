<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishFlavorsMapper">
<insert id="saveAll" >
    insert into sky_take_out.dish_flavor (dish_id, name, value)
    VALUES <foreach collection="dishFlavors" item="flavors" separator=",">
    (#{flavors.dishId},#{flavors.name},#{flavors.value})
</foreach>
</insert>
    <delete id="deleteByIdConnections">
        delete
        from dish_flavor
         where dish_flavor.dish_id in <foreach collection="ids" item="id" separator="," open="(" close=")">
        #{id}
    </foreach>
    </delete>
    <select id="selectByDishId" resultType="com.sky.entity.DishFlavor">
        select * from dish_flavor where dish_id=#{id}
    </select>

</mapper>
